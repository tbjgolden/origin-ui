

export { SIZE } from "../input/index.js";

export const TYPE = {
  select: "select",
  search: "search",
};

export const STATE_CHANGE_TYPE = Object.freeze({
  select: "select",
  remove: "remove",
  clear: "clear",
});
